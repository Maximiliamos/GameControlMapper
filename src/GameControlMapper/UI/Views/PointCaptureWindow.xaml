<Window x:Class="GameControlMapper.UI.Views.PointCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#66000000"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Maximized"
        Cursor="Cross"
        KeyDown="Window_KeyDown"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Canvas>
        <TextBlock Text="Щёлкните, чтобы указать центр. Esc — отмена."
                   Canvas.Left="24"
                   Canvas.Top="24"
                   Foreground="White"
                   FontSize="18"
                   FontWeight="SemiBold" />
    </Canvas>
</Window>
