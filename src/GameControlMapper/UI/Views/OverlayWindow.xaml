<Window x:Class="GameControlMapper.UI.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:GameControlMapper.ViewModels"
        Title="Схема зон управления"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Maximized"
        IsHitTestVisible="False">
    <Canvas>
        <ItemsControl ItemsSource="{Binding Bindings}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}" />
                    <Setter Property="Canvas.Top" Value="{Binding Y}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type viewModels:BindingViewModel}">
                    <Border Width="{Binding Width}"
                            Height="{Binding Height}"
                            Background="{Binding Color, Converter={StaticResource HexBrushConverter}}"
                            BorderBrush="#F8FAFC"
                            BorderThickness="2"
                            CornerRadius="4"
                            Opacity="{Binding Opacity}">
                        <Grid>
                            <Ellipse Width="7" Height="7" Fill="#F8FAFC" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Hotkey}" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" />
                                <TextBlock Text="{Binding Name}" FontSize="11" HorizontalAlignment="Center" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Canvas>
</Window>
