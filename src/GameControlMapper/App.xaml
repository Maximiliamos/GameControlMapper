<Application x:Class="GameControlMapper.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:GameControlMapper.UI.Converters">
    <Application.Resources>
        <converters:HexBrushConverter x:Key="HexBrushConverter" />
        <converters:BooleanLabelConverter x:Key="BooleanLabelConverter" />
        <converters:BindingKindLabelConverter x:Key="BindingKindLabelConverter" />

        <Color x:Key="ShellBackgroundColor">#10131A</Color>
        <Color x:Key="PanelBackgroundColor">#171B24</Color>
        <Color x:Key="PanelAltColor">#202633</Color>
        <Color x:Key="AccentColor">#4CC9F0</Color>
        <Color x:Key="TextColor">#E8EDF5</Color>
        <Color x:Key="MutedTextColor">#9AA7B8</Color>

        <SolidColorBrush x:Key="ShellBackgroundBrush" Color="{StaticResource ShellBackgroundColor}" />
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="{StaticResource PanelBackgroundColor}" />
        <SolidColorBrush x:Key="PanelAltBrush" Color="{StaticResource PanelAltColor}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="{StaticResource MutedTextColor}" />

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#202938" />
            <Setter Property="BorderBrush" Value="#303A4C" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="Segoe UI Semibold" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="MinHeight" Value="36" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,3,0,10" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#CC111722" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="CaretBrush" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="MinHeight" Value="34" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,3,0,10" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#273244" />
        </Style>
    </Application.Resources>
</Application>
